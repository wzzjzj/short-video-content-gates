# 1.7.10 — 2026-09-12

- 首次完整预览前优先调用项目已接入的完整检查；未接入该入口的环境继续直接使用预览包检查，避免重复执行同一检查。
- 收拢重复的阶段说明，并明确详细依据写入 BRIEF，PUBLISH 只保存发布资料、总结论和报告指针。

# 1.7.9 — 2026-09-09

- 新增统一预览入口 `check-preview-package.py`，一次串联实际时间线布局检查、播放遮罩、3:4封面裁切、主页缩略图和发布资料字段检查。
- 自动使用本次运行生成的代表帧与布局报告，拒绝旧截图、旧报告和已有输出目录；失败保留 `BLOCK` 或 `INCOMPLETE`，成功只到 `READY_FOR_VISUAL_REVIEW`，不自动写质量 PASS。
- 保留人工连续观看、实际封面核对和按需严格发布审计边界。

# 1.7.8 — 2026-09-09

- 修正实际 HyperFrames 页面时间线登记表初始化与诊断，兼容内部标记并继续拒绝真正的多时间线页面。
- 明确首版布局复用现成安全区样式，场景内全部可读文字（含片尾）必须放入安全容器。
- 发布资料模板括号内的填写提示不再被误当作字段名，避免“必要声明：无”误报缺项。

# 1.7.7 — 2026-09-08

- 用安装的HyperFrames运行时采样实际可见文字边界，合并输出代表帧；未接信息层、越界返回BLOCK，无发现不代表质量PASS。
- 安全证据使用实际交付的9:16/3:4封面，可核对源图居中裁切；按项目现有模板检出发布资料缺项。
- 保留静态诊断按需、人工连续观看和按需严格发布边界，不改官方技能或平台坐标。

# Changelog

## 1.7.6

- 画面验收方法统一到gate-model，入口和记录契约只引用；全局听审重点和换计划重绑定语义明确。

- 静态可见性检查改为按需诊断，撤销首次完整预览前的强制调用。
- 删除重复验收提醒；保留原有实际画面、安全区和封面验收要求。


## 1.7.5

- 增加只读 HTML 静态预检 `check-visible-layout.mjs`，识别隐藏、透明、1 像素或被裁切的配置绑定。
- 抖音 1080×1920 页面复用唯一共享坐标基线，检查实际文字及 Logo/显式可读图像的静态边界。
- 明确 `NO_STATIC_FINDINGS` 不能替代动画、素材、音频、封面及连续播放审查；脚本生成内容或外部样式缺失时返回未完整验证。
- 要求安全区结论对应已查看的实际证据；证据图已有遮挡反例时不得记录 PASS。

## 1.7.4

- 将抖音安全区集中为共享 JSON 基线，首版排版可由 `safe-layout.mjs` 生成信息层 CSS。
- 增加 `make-safe-area-evidence.py`，生成实际代表帧的播放页遮罩、3:4 封面裁切和主页缩略图。
- 补充场景主张与图片、旁白及文字的对应检查，以及代表帧对不同布局和运动极值的覆盖要求。
- 保持普通制作的集中内部修订和按需严格发布审计，不接管本地交付、用户确认或制作状态。

## 1.6.0

- 将完整预览包质量检查前移到第一次交付前。
- 严格发布审计改为仅在用户明确要求时启用。
- 手机发布和数据回填不作为本地制作完成条件。

早期版本的详细变更可查阅 GitHub 提交历史。
