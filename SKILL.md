---
name: short-video-content-gates
description: 分析参考视频并提炼可迁移机制，以及评估跨项目、跨工具的短视频立项、内容实质、脚本和完整预览前质量；仅在用户明确要求判断能否发布或由智能体执行外部发布时启用严格发布审计。用于参考热门或竞品视频策划内容，或创建、审查、修改、制作和准备发布短视频时；纯转码、混音等不改变内容决策的技术操作不触发。
metadata:
  version: "1.7.7"
  short-description: 跨项目短视频内容决策与质量门禁
---

# 短视频内容门禁

## 目的

把短视频的内容判断变成可记录、可复核的质量意见，并只对“可发布”声明或由智能体执行的外部发布提供硬停止；同时防止“技术检查通过、内容却只有问题复述、工具名或空泛口号”，以及关键文字被平台界面或裁切遮挡。该 Skill 是跨项目、跨工具的方法层，不拥有账号定位、项目事实、商业事实源、账号数据阈值、具体制作流程、用户确认点或制作任务完成状态。

## 职责边界

本 Skill 只负责：

1. 在用户提供参考视频或明确要求借鉴热门/竞品视频时，分析其内容、节奏、结构、画面职责和有效机制，区分可迁移原则与不可复制元素，并转化为当前账号可用的差异化方向。
2. 检查公共需求、前 2 秒进入理由、前 5 秒收益承诺、正文单一命题兑现、内容实质交付和后续行为接力。
3. 在日常制作的 intake、script-review、pre-production 阶段给出 PASS、REVISE、BLOCK 或 EXCEPTION；`pre-publish` 和 `post-publish` 只在用户明确提出相应请求时按需执行。
4. 对抖音 1080×1920 视频执行统一的平台 UI 安全区门禁；所有账号都必须避开顶部、右侧操作栏、左下及底部信息区，该平台基线不因个人号、门店号、是否挂团购而改变。
5. 要求读取当前项目已有的账号规则、适配层、数据基线和权威事实源；除已获批准的平台 UI 基线外，不得把项目路径、商业事实或账号数据阈值写成共享常量。
6. 要求把参考分析和本条视频的门禁依据写回项目既有 BRIEF、发布验收或等价记录，不能只写“已执行本 Skill”。

本 Skill 不负责：

- 代替项目决定账号定位、目标人群、品牌语气或商业转化路径。
- 规定统一播放量、跳出率、完播率、时长或互动率阈值。
- 核实某家门店的价格、设备、优惠、地址、活动或营业时间。
- 生成完整创意、脚本、分镜、素材、剪辑、字幕、封面或成片。
- 下载、抓取或绕过平台限制获取参考视频；用户未提供可分析的视频、合法本地文件或可访问来源时，只能说明输入缺口。
- 把参考视频的高互动或热门标签当作因果证明，或承诺复制其数据结果。
- 复制参考视频的具体文案、镜头序列、人物表演、品牌识别、受保护素材或独特表达。
- 承诺爆款，或把单条高数据自动升级成共享规律。
- 新建平行的“五道闸门.md”“爆款规则.md”或独立门禁结果文件。

## 开始前

1. 确认当前项目、账号、平台、任务阶段和本次允许的写入范围。
2. 按项目开工规则读取账号适配层、当前数据基线、商业事实源和已有 BRIEF、脚本、分镜、成片或发布记录。
3. 若项目没有数据基线，明确记录“当前未建立”，不得借用其他账号阈值。除非项目本地规则要求，或商业事实无法核实，否则缺少数值基线本身不自动 BLOCK。
4. 按当前阶段读取（制作时一并读取项目指定发布模板，作为本次发布资料字段来源）：
   - references/gate-model.md
   - references/result-contract.md
   - 只有用户明确要求判断“能否发布”或要求智能体执行外部发布时，才读取 references/release-evidence-schema.md 并执行 `pre-publish`。
5. 抖音 1080×1920 的首版布局与预览验收统一执行 references/gate-model.md 的“首版布局”和“实际画面验收”；HTML疑点诊断见同文件的按需章节。
6. 若任务包含分析参考视频、竞品视频、热门模板或“提取优点”，还必须读取 references/reference-analysis.md，并先完成该模式再进入 intake。参考分析不是 intake PASS，不能替代本账号的需求和事实核验。
7. 若用户只要求分析或审计，仅在回答中报告，不写回项目。

## 参考视频前置模式

`reference-analysis` 是按需执行的研究模式，不是第六道门禁。它回答“这个参考视频做对了什么、证据在哪里、哪些机制能迁移、迁移后必须改变什么”，不回答“照着做是否一定会火”。

1. 只在用户提供参考视频、允许访问的来源，或明确要求分析热门/竞品视频时触发。
2. 按 references/reference-analysis.md 完成输入核验、参数/转录/抽帧或等价证据采集、时间结构、论点—证据—画面对齐、可迁移/不可复制边界和账号适配。
3. 信息不足时输出 `PARTIAL` 并列出缺口；不得凭标题、封面、单张截图或平台标签臆造全片结构。
4. 分析结果写入 references/result-contract.md 规定的“参考视频分析”区域。用户只授权只读时，仅在回答中交付。
5. 参考分析完成后，仍须以当前账号、平台、目标观众和唯一核心承诺重新执行 intake；不得把参考作者的受众、事实、阈值或成功结果直接移植。

## 阶段门禁

| 阶段 | 必查内容 | 质量结论的含义 |
|---|---|---|
| intake | 需求证据、目标观众、唯一核心承诺、项目适配 | PASS 表示方向成立；REVISE、BLOCK 或 EXCEPTION 记录内容风险与建议，不决定本地制作授权 |
| script-review | 五道闸门、可定位的前 2 秒与前 5 秒、单一命题；按内容类型检查明确答案或论点、证据或推理、可执行方法或目标回报、适用边界 | PASS 表示脚本质量就绪；其他结论用于当前制作中的修订或风险标注，不新增用户确认点 |
| pre-production | 在第一次提交完整预览前解决脚本缺口，核对实际渲染预览、封面或首屏、标题、文案、话题及必要声明；确认内容兑现、事实可用、关键文字平台 UI 安全区、3:4 封面裁切和主页缩略图均可用 | PASS 表示完整预览包质量就绪；REVISE 直接指导内部最小修订，不新增用户确认点 |
| pre-publish（按需） | 仅在用户明确要求判断“能否发布”或要求智能体执行外部发布时，以待发布成片和封面的哈希及发布资料执行严格复核 | PASS 后才可标记“可发布”或由智能体执行发布；不得成为日常本地制作、交付或完成条件 |
| post-publish（按需） | 仅在用户明确要求复盘或主动提供数据时执行 | 完成当前账号观察记录，不自动改写共享规则 |

只评估当前任务已有材料所对应的阶段，不为满足本 Skill 而创建强制阶段链、用户确认点或制作状态。普通视频制作应在第一次完整预览交付前把内容兑现、实际画面、封面或首屏、标题、文案、话题、必要声明和平台适配一次检查并完成内部修订；不创建默认 `pre-publish` 待办，也不把它换名为另一道完成门槛。用户已授权制作时，REVISE 应直接指导当前制作中的最小修订。只有用户明确触发按需发布审计时，`pre-publish PASS` 才是标记“可发布”或由智能体执行外部发布的前提。EXCEPTION 必须有明确批准者、理由、范围和日期，且不能绕过平台 UI。

## 执行方式

1. 把五道闸门和内容实质用于当前方案与实现，合并记录判断、证据位置和必要修订；不按每个阶段重抄整张问卷。
2. 依 references/gate-model.md 判定 BLOCK、REVISE 和观察项，不把预测值伪装成确定事实。
3. 依 references/result-contract.md 保存本次结果；方法保存在 Skill，项目只保存这一次的判断证据。
4. 参考视频任务先产出可复核的分析结果，再将其中的可迁移机制作为 intake 的一项输入；不能从参考视频直接跳到脚本或制作。
5. 日常制作汇总问题后集中修订，只复核受改动影响的内容；未变化的事实与证据沿用。质量结论不接管制作授权、用户确认或任务完成；缺口未解决时如实标注，不伪造 PASS。严格发布审计沿用上表的按需边界。
7. post-publish 不是每条视频的必经阶段。只有用户明确要求复盘或主动提供数据时，才形成当前账号观察、项目内重复证据或跨账号候选规律；只有用户明确批准，才可修改本 Skill。
8. 抖音项目按 references/gate-model.md 的唯一坐标基线执行；项目文件只记录本条视频的检查对象、证据和结论，不复制坐标规则。
9. 画面与封面检查执行 references/gate-model.md 的“实际画面验收”，结果按 references/result-contract.md 保存。
10. 只有用户明确触发严格发布审计时，才按 references/release-evidence-schema.md 保存机器证据并用 `scripts/check-release.mjs --video <视频目录>` 验证。普通本地制作和交付不生成该证据，也不保留“待发布检查”待办。
11. 本 Skill 只记录质量门禁、必要的检查对象事实和“门禁绕过”事故，不维护制作状态或决定任务完成；文件已经生成、交付或发布不能反向把门禁改成 PASS，门禁 BLOCK 也不能抹掉已经发生的事实。
12. 只有项目提供自动 `publish` 脚本且用户明确要求由智能体执行发布时，才调用 `scripts/install-release-hook.mjs --project-root <项目根> --video <视频目录>` 安装 npm `prepublish` 钩子。安装器发现既有不同的 `prepublish` 时必须停止，不得覆盖。用户自行在手机端发布不需要安装该钩子。

## 规则治理

证据升级顺序：

单条观察 → 当前账号重复验证 → 多账号候选规律 → 人工批准 → 共享规则变更

不得自学习、自修改本 Skill，不得因一次异常爆款或失败样本污染其他项目。

## 外部参考治理

外部视频框架只能作为方法参考，不能成为本 Skill 的运行依赖或自动更新源。当前内容实质增强参考 OpenMontage 截至 2026-08-28 的公开设计，包括“研究材料 → 交付承诺 → 脚本 → 场景计划 → 交付审查”的分层思想；本 Skill 未复制其源码或大段文本。

- 上游参考：[OpenMontage](https://github.com/calesthio/OpenMontage)、[animated-explainer pipeline](https://github.com/calesthio/OpenMontage/blob/main/pipeline_defs/animated-explainer.yaml)、[reviewer](https://github.com/calesthio/OpenMontage/blob/main/skills/meta/reviewer.md)。
- OpenMontage 停更、改名、改许可或不可访问时，本 Skill 仍须独立执行。
- 上游更新不得自动写入本 Skill。只有在核对变更、许可证、项目适配和至少一个真实样例后，才能提出选择性吸收；共享规则变更仍需用户明确批准并更新版本号。
- 外部框架与当前项目规则冲突时，以用户要求、项目权威事实源、平台边界和本 Skill 已批准规则为准。
